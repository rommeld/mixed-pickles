# TODO

- [ ] Add dedicated tests for NUL-delimited git log parsing edge cases
- [ ] Add tests for terminal-output sanitization of control characters
- [ ] Add integration test coverage for branch-filter behavior in detached HEAD vs git failures
